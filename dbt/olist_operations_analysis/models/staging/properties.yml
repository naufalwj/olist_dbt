version : 2

models:
    - name: stg_customers
      description: "This is a staging model for the customers table from olist_customers_dataset"
      columns:
        - name: customer_id
          data_tests:
            - unique
            - not_null
        - name: customer_unique_id
          data_tests:
            - unique
            - not_null
